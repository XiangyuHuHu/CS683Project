package com.example.myapplication

/*
class EditCardFragment {
    private lateinit var rankSpinner: Spinner
    private lateinit var suitSpinner: Spinner
    private lateinit var confirmButton: Button

    override fun onCreateView(
        inflater: LayoutInflater, container: ViewGroup?,
        savedInstanceState: Bundle?
    ): View? {
        return inflater.inflate(R.layout.fragment_edit_card, container, false)
    }

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)

        rankSpinner = view.findViewById(R.id.rankSpinner)
        suitSpinner = view.findViewById(R.id.suitSpinner)
        confirmButton = view.findViewById(R.id.confirmButton)

        confirmButton.setOnClickListener {
            val selectedRank = rankSpinner.selectedItem.toString()
            val selectedSuit = suitSpinner.selectedItem.toString()
            // Here, you can pass this information back to your MainActivity
            // Or wherever you need it using interfaces, sharedViewModel, etc.
        }
    }
}
*/
